# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
#             - nombre: Actualizador de canalización de Pachyderm
  # Puedes anclar al commit exacto o a la versión.
  # usos              - nombre: Actualizador de canalización de Pachyderm
  # Puedes anclar al commit exacto o a la versión.
  # usos: paquidermo/paquidermo-acciones@69c4694668a732392b203fab5b8dbdc14b8a097c
  usos: pachyderm/pachyderm-actions@0.0.1
                          
  # Puedes anclar al commit exacto o a la versión.
  # usos: paquidermo/paquidermo-acciones@69c4694668a732392b203fab5b8dbdc14b8a097c
  usos: pachyderm/pachyderm-actions@0.0.1
               - nombre: Actualizador de canalización de Pachyderm
  # Puedes anclar al commit exacto o a la versión.
  # usos: paquidermo/paquidermo-acciones@69c4694668a732392b203fab5b8dbdc14b8a097c
  usos: pachyderm/pachyderm-actions@0.0.1
          
# Th            - nombre: Actualizador de canalización de Pachyderm
  # Puedes anclar al commit exacto o a la versión.
  # usos: paquidermo/paquidermo-acciones@69c4694668a732392b203fab5b8dbdc14b8a097c
  usos: pachyderm/pachyderm-actions@0.0.1
          is workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml

    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    - name: Update dependency graph
      uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
